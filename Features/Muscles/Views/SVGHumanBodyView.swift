import SwiftUI
import SVGView   // SPM: https://github.com/exyte/SVGView

/// Displays one torso SVG (front or back). Responsible only for loading & cropping.
/// We'll add *highlighting* here later, without touching callers.

